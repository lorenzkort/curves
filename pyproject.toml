[project]
name = "curves"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rasterio>=1.5.0",
    "ruff>=0.15.17",
    "poethepoet>=0.46.0",
]

[tool.poe.tasks]
lint   = "uv run ruff check ."
